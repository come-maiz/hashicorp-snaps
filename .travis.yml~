sudo: required

services:
  - docker

script:
  docker run -v $(pwd):/cwd snapcore/snapcraft sh -c 'apt install software-properties-common -y && add-apt-repository -y ppa:gophers/archive && apt update && cd /cwd && snapcraft
